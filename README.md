# suvi++ (website)

brand new in development official website of suvi. consists of frontend made with svelte/sveltekit, backend made with rust/axum, database running mongodb.

## running

before you run the frontend, you must run `npm install` in the frontend/ subdirectory. from there you can `docker compose up --build`, or `docker compose up` to skip compiling the rust backend.